#include "MeshMgr.h"

void MeshMgr::Add(std::string fileName)
{
	/*
	Assimp::Importer importer;
	const aiScene* mScene = importer.ReadFile(fileName, aiProcess_Triangulate);

	assert(mScene == nullptr);

	ProcessNode(mScene->mRootNode, mScene);
	*/
}

void MeshMgr::ProcessNode(/*aiNode* node, const aiScene* scene*/)
{
	/*
	for (UINT i = 0; i < node->mNumMeshes; ++i)
	{
	}
	*/
}
